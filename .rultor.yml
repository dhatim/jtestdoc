decrypt:
  settings.xml: "JTestDoc/settings.xml.asc"
  pubring.gpg: "JTestDoc/pubring.gpg.asc"
  secring.gpg: "JTestDoc/secring.gpg.asc"
release:
  script: |
    mvn versions:set "-DnewVersion=${tag}"
    git commit -am "${tag}"
    mvn clean deploy --settings /home/r/settings.xml